-- =======================================================================
-- Create federation template for a Windows Azure SQL Database
-- =======================================================================

CREATE FEDERATION <Federation_Name, sysname, Federation_Name>
(<Distribution_Name, sysname, Distribution_Name> <Data_Type, systype, BIGINT> RANGE)
GO
